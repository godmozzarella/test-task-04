# Online Booking

Страница онлайн-бронирования столика в ресторане: форма с валидацией, имитация отправки и экран подтверждения. Тестовое задание для SAVEUR (Junior Frontend).

**Превью:** <тут будет ссылка>

## Стек

- **Next.js 16** (App Router) + **React 19**
- **TypeScript** — типизированы props, state и обработчики
- **SCSS Modules** — изоляция стилей + переменные палитры
- **react-hook-form** — управление формой и валидацией
- **ESLint + Prettier** — линтинг и форматирование
- **Vitest** — юнит-тесты валидации и маски телефона

## Запуск локально

Требуется **Node.js 20+** (Next.js 16).

```bash
# установить зависимости
npm install

# запустить дев-сервер
npm run dev
```

Открой [http://localhost:3000](http://localhost:3000).

Прочие команды:

```bash
npm run build   # production-сборка
npm start       # запуск собранного приложения
npm run lint    # проверка ESLint
npm run format  # форматирование Prettier
npm test        # юнит-тесты (Vitest)
```

## Структура проекта

```
src/
  app/
    layout.tsx                 # корневой layout, шрифты, метаданные
    page.tsx                   # связка формы и подтверждения через состояние
    page.module.scss
    globals.css                # базовый reset и фон
  components/
    BookingForm/
      BookingForm.tsx          # форма на react-hook-form
      Field.tsx                # обёртка поля: лейбл + контрол + ошибка
      BookingForm.module.scss
    ConfirmationScreen/
      ConfirmationScreen.tsx   # экран успешного бронирования
      ConfirmationScreen.module.scss
  types/
    booking.ts                 # BookingFormData, BookingStatus
  utils/
    constants.ts               # слоты времени, лимиты, задержки
    date.ts                    # ISO-даты и форматирование
    validation.ts              # чистые валидаторы полей
    validation.test.ts         # юнит-тесты валидаторов
    phone.ts                   # маска телефона
    phone.test.ts              # юнит-тесты маски
  styles/
    _variables.scss            # палитра и токены
    _mixins.scss               # fluid-text миксин
```

## Принятые решения

- **App Router** вместо Pages Router — это актуальный подход Next.js, удобная файловая структура и серверные компоненты по умолчанию.
- **SCSS Modules** — изоляция классов исключает конфликты стилей между
  компонентами, а переменные SCSS централизуют палитру. Не выбрал обычный CSS или Tailwind, так как с SCSS Modules у меня сильно больше опыта и в примере ТЗ используется как раз такой подход.
- **Валидаторы** - функции без зависимости от реакта, легко тестировать и переиспользовать.
- **react-hook-form, `mode: 'onBlur'`** — закрывает требование ТЗ показывать ошибки при потере фокуса. Валидаторы подключаются через `validate`, разметка остаётся чистой.
- **Машина состояний `BookingStatus`** — `page.tsx` владеет состоянием `idle - loading - success`: ставит `loading`, имитирует запрос, затем `success` с данными брони. Форма не знает про задержку — принимает `onSubmit` и `isSubmitting` пропами. Так ответственность разделена: форма отвечает за валидацию и сбор данных, страница — за асинхронность и переключение экранов.
- **Константы** - в constants.ts вынесены все числа-константы (слоты, лимиты, задержка), чтобы не было "магических чисел" и правила менялись в одном месте
- **Даты** - сравниваются как ISO-строка (YYYY-MM-DD), а не через Date — это исключает баги с часовыми поясами.
- **Время и гости** - через `<select>` с фиксированными опциями: значение физически не может быть невалидным.
- **Field** - вынесено в отдельный компонент для удобного переиспользования и избежания дублирования в коде. 
- **Палитра** — в SCSS-переменных (_variables.scss), подключается в модули через относительный @use.
- **Анимация перехода между формой** — лёгкий `@keyframes fade-in`: не перегружает и не нарушает стилистику.
- **Детали по бронированию** рендерятся из массива (label + value), а не пятью повторами разметки — меньше дублирования.
- **ConfirmationScreen** — без состояния: получает данные пропсами, всё управление снаружи.
- **Форматирование номера при вводе** - добавил при вводе номера маску для российского номера, где при вводе с 8 или 7 в начало сразу ставится +7 после чего весь номер подгоняется под маску "+7 (999) 676-67-67". Но тк я не использовал доп библиотеку о которой я узнал (react-imask), есть микро баг, когда стираешь номер стереть "-" или ")" не получится, не удалив цифру. Можно сделать идеально с доп библиотекой, но я оставлю это на будущее улучшение. 
- **Единицы измерения** - для шрифтов, отступов, гэпов, размеров элементов использовал rem, чтобы происходил правильный скейл элементов, когда пользователь меняет размер шрифта в браузере. Не трогал бордеры тени и все, что не должно поплыть. 
- **Mixin функция** - размер заголовка плавно тянется между минимумом и максимумом через SCSS-миксин с `clamp()`. 
- **Нативные контролы** — `appearance: none` применён только к `<select>` (фикс высоты в Safari + своя стрелка), инпуты оставлены нативными, чтобы у даты работала встроенная подсказка формата.
- **Почему Vitest** - Нативно работает с Vite/ESM и TypeScript; Очень быстрый — переиспользует esbuild, прогон 27 тестов ~0.2с. Jest тоже подошёл бы, но в проекте на современном тулинге Vitest даёт меньше конфигурации и быстрее фидбэк.
- **Тесты на Vitest** — покрыты чистые функции: все валидаторы (`validation.ts`) и маска телефона (`phone.ts`), включая граничные случаи (дата сегодня / +90 дней, гости 1 и 12) и нормализацию `8 в +7`. Для тестов даты "сегодня" заморожено через `vi.setSystemTime`, чтобы прогон был стабилен во времени. Логика валидации намеренно вынесена из компонентов чистыми функциями — именно поэтому её можно тестировать без рендера и DOM.

## Что бы доделал при наличии времени

- **Полноценная анимация перехода** между формой и подтверждением (exit + enter, например через Framer Motion `AnimatePresence`) — сейчас реализовано базовое появление через CSS `fade-in`.
- **Доступность**: связать сообщения об ошибках с полями через `aria-describedby` и фокусировать первое невалидное поле при отправке (`shouldFocusError`).
- **Маска телефона на `react-imask`** — убрать микро-баг со стиранием разделителей, который остаётся в самописной маске.
- **Тесты компонентов и e2e**: React Testing Library для формы (сценарии валидации и отправки) и Playwright для сквозного прохода.
- **Слой отправки**: заменить `setTimeout`-заглушку на изолированный сервис с обработкой ошибок сети и состоянием `error` в `BookingStatus`.
- **Блокировка всей формы во время отправки** (`fieldset disabled`), а не только кнопки.

